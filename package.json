{
  "name": "docs",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "yarn generate-examples && yarn build:no-examples",
    "build:no-examples": "yarn check-links && yarn translate && yarn package-code && docusaurus build",
    "check-links": "yarn markdown-link-check -c markdown-link-check.config.json -q docs/**/*.md",
    "swizzle": "docusaurus swizzle",
    "deploy": "yarn run package-code && docusaurus deploy",
    "clear": "docusaurus clear",
    "generate-examples": "cd scripts && yarn install && yarn generate",
    "translate:core_features": "cd code_examples/core_features && yarn translate",
    "translate:dapp_examples": "cd code_examples/dapp && yarn translate",
    "translate:workshop": "cd code_examples/workshop && yarn translate",
    "translate": "yarn translate:core_features && yarn translate:dapp_examples && yarn translate:workshop",
    "serve": "docusaurus serve",
    "package-code": "node ./src/utilities/archive.js --folders workshop",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "^2.1.0",
    "@docusaurus/preset-classic": "^2.1.0",
    "@docusaurus/remark-plugin-npm2yarn": "^2.1.0",
    "@mdx-js/react": "^1.6.21",
    "archiver": "^5.3.0",
    "clsx": "^1.1.1",
    "markdown-link-check": "^3.10.2",
    "mdx-mermaid": "^1.2.2",
    "mermaid": "^9.0.0",
    "node-args": "^2.1.8",
    "raw-loader": "^4.0.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-markdown": "^8.0.3"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}