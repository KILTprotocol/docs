/* eslint-disable @typescript-eslint/no-unused-vars */
export async function main() {
  const MAX_ACCEPTED_AGE = 60000 // ms -> 1 minute
  const MIN_ACCEPTED_AGE = -1000 // allow for some imprecision in system time
  const submissions = new Map()
}
